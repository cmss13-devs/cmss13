/// (gold_amount)
#define COMSIG_MOBA_GIVE_GOLD "moba_give_gold"

/// (xp_amount)
#define COMSIG_MOBA_GIVE_XP "moba_give_xp"

/// (list/datum/moba_item/items)
#define COMSIG_MOBA_GET_OWNED_ITEMS "moba_get_owned_items"

/// (list/gold_holder)
#define COMSIG_MOBA_GET_GOLD "moba_get_gold"

/// (list/level_holder)
#define COMSIG_MOBA_GET_LEVEL "moba_get_level"

/// (datum/moba_item/item)
#define COMSIG_MOBA_ADD_ITEM "moba_add_item"

/// (datum/moba_item/item)
#define COMSIG_MOBA_REMOVE_ITEM "moba_remove_item"

/// (level)
#define COMSIG_MOBA_LEVEL_UP "moba_level_up"

/// (list/ap_holder)
#define COMSIG_MOBA_GET_AP "moba_get_ap"

#define COMSIG_MOBA_ACTION_ADD_LEVELUP "moba_action_add_levelup"

#define COMSIG_MOBA_ACTION_REMOVE_LEVELUP "moba_action_remove_levelup"

/// (list/lifesteal_holder)
#define COMSIG_MOBA_GET_LIFESTEAL "moba_get_lifesteal"

/// (lifesteal)
#define COMSIG_MOBA_SET_LIFESTEAL "moba_set_lifesteal"

#define COMSIG_XENO_PHYSICAL_ABILITY_HIT "xeno_physical_ability_hit"

/// (list/datum_holder)
#define COMSIG_MOBA_GET_PLAYER_DATUM "moba_get_player_datum"

/// (list/bhp_holder)
#define COMSIG_MOBA_GET_BONUS_HP "moba_get_bonus_hp"

/// (mob/living/minion)
#define COMSIG_MOBA_MINION_SPAWNED "moba_minion_spawned"

/// (mob/living/attacked)
#define COMSIG_MOB_ALIEN_ATTACK "mob_alien_attack"

/// (mob/living/stunned)
#define COMSIG_MOBA_STUN_GIVEN "moba_stun_given"
