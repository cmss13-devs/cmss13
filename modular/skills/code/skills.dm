/datum/skills/intel/New(mob/skillset_owner)
	. = ..()
	skills |= list(
		SKILL_VEHICLE = SKILL_VEHICLE_LARGE,
	)

/datum/skills/pilot/New(mob/skillset_owner)
	. = ..()
	skills |= list(
		SKILL_VEHICLE = SKILL_VEHICLE_CREWMAN,
	)

/datum/skills/crew_chief/New(mob/skillset_owner)
	. = ..()
	skills |= list(
		SKILL_VEHICLE = SKILL_VEHICLE_LARGE,
	)

/datum/skills/MP/New(mob/skillset_owner)
	. = ..()
	skills |= list(
		SKILL_VEHICLE = SKILL_VEHICLE_SMALL,
	)

/datum/skills/MW/New(mob/skillset_owner)
	. = ..()
	skills |= list(
		SKILL_VEHICLE = SKILL_VEHICLE_SMALL,
	)

/datum/skills/provost/New(mob/skillset_owner)
	. = ..()
	skills |= list(
		SKILL_VEHICLE = SKILL_VEHICLE_SMALL,
	)

/datum/skills/OT/New(mob/skillset_owner)
	. = ..()
	skills |= list(
		SKILL_VEHICLE = SKILL_VEHICLE_SMALL,
	)

/datum/skills/MT/New(mob/skillset_owner)
	. = ..()
	skills |= list(
		SKILL_VEHICLE = SKILL_VEHICLE_SMALL,
	)

/datum/skills/mess_technician/New(mob/skillset_owner)
	. = ..()
	skills |= list(
		SKILL_VEHICLE = SKILL_VEHICLE_SMALL,
	)

/datum/skills/CT/New(mob/skillset_owner)
	. = ..()
	skills |= list(
		SKILL_VEHICLE = SKILL_VEHICLE_SMALL,
	)

/datum/skills/tl/New(mob/skillset_owner)
	. = ..()
	skills |= list(
		SKILL_VEHICLE = SKILL_VEHICLE_SMALL,
	)

/datum/skills/CMO/New(mob/skillset_owner)
	. = ..()
	skills |= list(
		SKILL_VEHICLE = SKILL_VEHICLE_SMALL,
	)

/datum/skills/CMP/New(mob/skillset_owner)
	. = ..()
	skills |= list(
		SKILL_VEHICLE = SKILL_VEHICLE_SMALL,
	)

/datum/skills/RO/New(mob/skillset_owner)
	. = ..()
	skills |= list(
		SKILL_VEHICLE = SKILL_VEHICLE_SMALL,
	)
