import { useBackend, useLocalState } from '../backend';

import {
  Button,
  Section,
  Flex,
  Box,
  Tooltip,
  Input,
  NoticeBox,
  Icon,
} from '../components';
import { Window } from '../layouts';
import { classes } from 'common/react';

export const SquadInfo = (props, context) => {
  return <Window>
    <Window.Content>
      squad stuff
    </Window.Content>
  </Window>
}
